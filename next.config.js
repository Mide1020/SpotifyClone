/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["tse4.mm.bing.net", "images.unsplash.com", "api.dicebear.com", "localhost"]
    }
}

module.exports = nextConfig
